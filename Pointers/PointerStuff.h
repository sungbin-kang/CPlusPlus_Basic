

// // a pointer to a function that takes a double
// // and returns the pointer to a function
// typedef double (*functionType) (double x);

// double area (double r);
// double circumference(double r);